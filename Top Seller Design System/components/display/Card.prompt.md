# Card
Surface container with hairline border + soft elevation.

```jsx
<Card variant="brand" padding="lg">…</Card>
<Card interactive>…</Card>
```
Variants: default, brand (morado), inverse (black), accent-edge (orange top rule). interactive adds hover lift.