# Stat
KPI / metric block with directional delta. For sales dashboards.

```jsx
<Stat label="Revenue" value="$48.2k" delta="12.4%" trend="up" icon={<Bolt/>} />
```
trend: up (green), down (red), flat.