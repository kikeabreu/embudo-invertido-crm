# IconButton
Square icon-only action for toolbars and compact rows.

```jsx
<IconButton variant="secondary" aria-label="Add"><Plus/></IconButton>
```
Always pass aria-label. Variants: primary, accent, secondary, ghost.