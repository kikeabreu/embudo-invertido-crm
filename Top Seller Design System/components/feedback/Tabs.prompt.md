# Tabs
Horizontal tab bar with orange active underline.

```jsx
<Tabs value={tab} onChange={setTab} tabs={[{id:'a',label:'Pipeline',badge:'24'}]} />
```
Controlled (value) or uncontrolled (defaultValue).