# Input
Text field with brand focus ring + optional leading icon.

```jsx
<Input placeholder="Search leads…" iconLeft={<Search/>} />
<Input invalid defaultValue="bad@" />
```
Sizes sm/md/lg. Set invalid for error state (danger border).